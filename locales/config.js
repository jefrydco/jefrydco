import { locales } from '../utils'

export default {
  baseUrl: 'https://jefrydco.id',
  seo: false,
  defaultLocale: 'id',
  detectBrowserLanguage: false,
  lazy: true,
  langDir: 'locales/',
  locales
}
